name: auto-merge

on:
  pull_request:

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: anass-at/dependbot@master
        with:
          target: patch
          github-token: ${{ secrets.PROTECTED_BRANCH_TOKEN }} 
